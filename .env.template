# Azure AI Foundry / Azure OpenAI endpoint settings
# Copy this file to .env and fill in your values

# ── Authentication (choose one) ───────────────────────────────────────────────
# Option 1 — API key (takes priority if set)
#   Found in: AI Foundry portal → your project → Overview → Keys and endpoints
# AZURE_API_KEY=<your-api-key>

# Option 2 — Entra ID / CLI (used when AZURE_API_KEY is not set)
#   Run `az login` locally; managed identity is used automatically when hosted.
#   No extra config needed.
# ─────────────────────────────────────────────────────────────────────────────

# The Azure OpenAI endpoint URL from your Foundry project
# Found in: AI Foundry portal → your project → Overview → Azure OpenAI endpoint
AZURE_API_BASE=https://<resource>.openai.azure.com/

# The API version to use
AZURE_API_VERSION=2024-12-01-preview

# The name of your model deployment in Azure AI Foundry
AZURE_DEPLOYMENT_NAME=gpt-4o
